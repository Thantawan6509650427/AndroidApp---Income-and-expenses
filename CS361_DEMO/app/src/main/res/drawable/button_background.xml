<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- สีพื้นหลังของปุ่ม -->
    <corners android:radius="5dp" /> <!-- ปรับ radius ตามที่ต้องการ -->
    <stroke
        android:width="2dp"
        android:color="@android:color/darker_gray" /> <!-- ขอบของปุ่ม -->
</shape>
